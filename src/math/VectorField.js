import { Vector3 } from "three";

class VectorField {
  //expact positions is tensor with shape [3,n]
  //return tensor with shape [3,n]
  get_vectors_tensor(time, positions) {
    
  }

  getVector(time, positions, optionalTarget = new Vector3()) {

  }
}

export { VectorField };
